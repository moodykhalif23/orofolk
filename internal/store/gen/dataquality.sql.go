// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: dataquality.sql

package gen

import (
	"context"
)

const catalogCompletenessSummary = `-- name: CatalogCompletenessSummary :one

WITH pa AS (
  SELECT p.id,
         ((p.attributes -> a.code) IS NOT NULL
          AND (p.attributes -> a.code) NOT IN ('null'::jsonb, '""'::jsonb, '[]'::jsonb, '{}'::jsonb)) AS present
  FROM products p
  JOIN attribute_family_attributes afa
    ON afa.family_id = p.attribute_family_id AND afa.is_required = true
  JOIN attributes a ON a.id = afa.attribute_id
  WHERE p.organization_id = $1 AND p.deleted_at IS NULL
),
per_product AS (
  SELECT id,
         count(*) AS req_total,
         count(*) FILTER (WHERE present) AS req_present
  FROM pa
  GROUP BY id
)
SELECT
  (SELECT count(*) FROM products pt
     WHERE pt.organization_id = $1 AND pt.deleted_at IS NULL)::int AS products_total,
  (SELECT count(*) FROM products pt
     WHERE pt.organization_id = $1 AND pt.deleted_at IS NULL AND pt.attribute_family_id IS NOT NULL)::int AS products_with_family,
  count(*)::int AS products_scored,
  COALESCE(avg(req_present::float8 / req_total) * 100, 100)::float8 AS avg_completeness,
  count(*) FILTER (WHERE req_present = req_total)::int AS complete_count,
  count(*) FILTER (WHERE req_present < req_total)::int AS incomplete_count
FROM per_product
`

type CatalogCompletenessSummaryRow struct {
	ProductsTotal      int32   `json:"products_total"`
	ProductsWithFamily int32   `json:"products_with_family"`
	ProductsScored     int32   `json:"products_scored"`
	AvgCompleteness    float64 `json:"avg_completeness"`
	CompleteCount      int32   `json:"complete_count"`
	IncompleteCount    int32   `json:"incomplete_count"`
}

// Data quality / catalog completeness (Platform roadmap, Phase 1). A product's
// "completeness" is how many of the REQUIRED attributes in its attribute family
// carry a meaningful value in the product's attributes JSONB. "Meaningful" means
// the key is present and the value is not JSON null, "", [] or {} — so a set
// multiselect or boolean-false counts as filled, an empty one does not. Products
// with no family, or a family with no required attributes, are simply not scored.
func (q *Queries) CatalogCompletenessSummary(ctx context.Context, organizationID int64) (CatalogCompletenessSummaryRow, error) {
	row := q.db.QueryRow(ctx, catalogCompletenessSummary, organizationID)
	var i CatalogCompletenessSummaryRow
	err := row.Scan(
		&i.ProductsTotal,
		&i.ProductsWithFamily,
		&i.ProductsScored,
		&i.AvgCompleteness,
		&i.CompleteCount,
		&i.IncompleteCount,
	)
	return i, err
}

const catalogCompletenessWorst = `-- name: CatalogCompletenessWorst :many
WITH pa AS (
  SELECT p.id, p.sku, p.name, a.code AS attr_code,
         ((p.attributes -> a.code) IS NOT NULL
          AND (p.attributes -> a.code) NOT IN ('null'::jsonb, '""'::jsonb, '[]'::jsonb, '{}'::jsonb)) AS present
  FROM products p
  JOIN attribute_family_attributes afa
    ON afa.family_id = p.attribute_family_id AND afa.is_required = true
  JOIN attributes a ON a.id = afa.attribute_id
  WHERE p.organization_id = $1 AND p.deleted_at IS NULL
)
SELECT id, sku, name,
       count(*)::int AS required_total,
       count(*) FILTER (WHERE present)::int AS required_present,
       COALESCE(array_agg(attr_code ORDER BY attr_code) FILTER (WHERE NOT present), '{}')::text[] AS missing
FROM pa
GROUP BY id, sku, name
HAVING count(*) FILTER (WHERE NOT present) > 0
ORDER BY count(*) FILTER (WHERE present)::float8 / count(*) ASC, name
LIMIT $2
`

type CatalogCompletenessWorstParams struct {
	OrganizationID int64 `json:"organization_id"`
	RowLimit       int32 `json:"row_limit"`
}

type CatalogCompletenessWorstRow struct {
	ID              int64    `json:"id"`
	Sku             string   `json:"sku"`
	Name            string   `json:"name"`
	RequiredTotal   int32    `json:"required_total"`
	RequiredPresent int32    `json:"required_present"`
	Missing         []string `json:"missing"`
}

func (q *Queries) CatalogCompletenessWorst(ctx context.Context, arg CatalogCompletenessWorstParams) ([]CatalogCompletenessWorstRow, error) {
	rows, err := q.db.Query(ctx, catalogCompletenessWorst, arg.OrganizationID, arg.RowLimit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CatalogCompletenessWorstRow
	for rows.Next() {
		var i CatalogCompletenessWorstRow
		if err := rows.Scan(
			&i.ID,
			&i.Sku,
			&i.Name,
			&i.RequiredTotal,
			&i.RequiredPresent,
			&i.Missing,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
